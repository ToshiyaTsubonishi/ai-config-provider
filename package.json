{
  "name": "ai-config-provider",
  "version": "1.0.0",
  "description": "Provider runtime that loads selected ai-config entries from a local bundle and exposes them as an MCP server.",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "bundle:from-ai-config": "tsx scripts/materialize-provider-bundle.ts",
    "pretest": "npm run build",
    "test": "vitest run",
    "start": "node dist/index.js",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^5.2.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.5.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  }
}
